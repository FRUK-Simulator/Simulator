import { createSlice } from "@reduxjs/toolkit";

/**
 * Reducers for handling the state of the log such as whether it is executing or not.
 */
export const controlHubLogSlice = createSlice({
  initialState: {},
  name: "controlHubLog",
  reducers: {},
});
