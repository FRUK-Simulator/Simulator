import styled from "styled-components";

/**
 * Content container with consistent padding.
 * Often used as a main container for a page or primary section.
 */
export const Content = styled.div`
  padding: 48px 70px;
`;
