import { render, screen } from "./testUtils";
import React from "react";
import { App } from "./App";

describe("App", () => {
  it("renders the VM controls", () => {
    //render(<App />);
    //expect(screen.getByText("Start"));
    //expect(screen.getByText("Next"));
    //expect(screen.getByText("Run"));
  });
});
