import { createSlice } from "@reduxjs/toolkit";

/**
 * Reducers for handling the state of the simulator such as whether it is executing or not.
 */
export const robotSimulatorReducer = createSlice({
    initialState: {},
    name: 'simulator',
    reducers: {}
});